        # Tends to follow version of main module, but not always.
$VERSION = '3.001';
#       $VERSION is value that Makefile.PL finds, so it must have 
#                the proper number.

# Reference:
#   http://modernperlbooks.com/mt/2009/07/version-confusion.html

1; #end
